## Reproduction Guidance

I cannot run these commands for you. Run them locally in an isolated checkout:

1. Install the tested version.
2. Create the fixture file described in `evidence.reproducer`.
3. Run `node repro.js`.

Paste the command output here so I can help record `evidence.observed_result`.
